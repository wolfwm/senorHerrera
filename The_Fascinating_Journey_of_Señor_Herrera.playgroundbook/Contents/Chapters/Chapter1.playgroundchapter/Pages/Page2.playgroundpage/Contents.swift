//#-hidden-code

import Foundation
import UIKit
import PlaygroundSupport

let window = UIWindow(frame : CGRect(x: 0, y: 0, width: UIScreen.main.bounds.width, height: UIScreen.main.bounds.height))
window.rootViewController = SmithingViewController()
window.makeKeyAndVisible()

PlaygroundPage.current.liveView = window

//#-end-hidden-code

/*:
 First we must heat Señor Herrera to about a 1100ºC.
 
 This heating process relieves tension that could be built up in the steel structure, it also makes the steel more malleable.
 
 Try placing and removing Señor Herrera in the forge to see what happens...
 */

//: [<Previous](@previous) | [Next >](@next)
